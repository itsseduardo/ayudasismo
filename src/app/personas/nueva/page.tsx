"use client";
import { createPerson } from "@/app/actions";import { input,label,ManagedForm } from "@/components/form-kit";import { PageHead } from "@/components/site";
export default function NewPerson(){return <div className="page"><PageHead eyebrow="Nuevo reporte" title="Reportar una persona sin contacto">Publica solo lo necesario. No incluyas documentos, diagnósticos ni direcciones exactas.</PageHead><ManagedForm action={createPerson} draftKey="draft-person" successPath={(id,t)=>`/personas/${id}/administrar?token=${t}`}><fieldset className="form-grid"><legend className="wide">Datos de la persona</legend><label className={label}>Nombre completo<input className={input} name="full_name" required maxLength={120}/></label><label className={label}>Edad aproximada<input className={input} name="approximate_age" type="number" min="0" max="120"/></label><label className={label}>Departamento<input className={input} name="department" required/></label><label className={label}>Municipio<input className={input} name="municipality" required/></label><label className={label}>Barrio, vereda o zona<input className={input} name="area"/></label><label className={label}>Último lugar donde fue vista<input className={input} name="last_seen_place"/></label><label className={label}>Último contacto<input className={input} name="last_contact_at" type="datetime-local"/></label><label className={`${label} wide`}>Descripción breve<textarea className={input} name="description" rows={4} required maxLength={600}/></label></fieldset><fieldset className="form-grid"><legend className="wide">Tu información</legend><label className={label}>Tu nombre<input className={input} name="reporter_name" required/></label><label className={label}>Relación con la persona<input className={input} name="relationship" required/></label><label className={label}>Teléfono o WhatsApp<input className={input} name="phone" type="tel" required/></label><label className="flex items-start gap-3"><input name="show_phone" value="true" type="checkbox" className="mt-1 size-5"/><span><b>Mostrar mi teléfono públicamente</b><small className="block muted">Si no marcas esto, permanecerá privado.</small></span></label><label className="wide flex items-start gap-3"><input name="consent" required type="checkbox" className="mt-1 size-5"/><span>Autorizo la publicación de este reporte y comprendo que es información comunitaria.</span></label></fieldset></ManagedForm></div>}
