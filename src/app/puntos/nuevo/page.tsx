"use client";
import { createPoint } from "@/app/actions";import { input,label,ManagedForm } from "@/components/form-kit";import { PageHead } from "@/components/site";
export default function NewPoint(){return <div className="page"><PageHead eyebrow="Nuevo punto" title="Publicar acopio o refugio">Incluye un teléfono si es posible y confirma que la información sea reciente.</PageHead><ManagedForm action={createPoint} draftKey="draft-point" successPath={(id,t)=>`/puntos/${id}?token=${t}`}><div className="form-grid"><label className={label}>Tipo<select className={input} name="type"><option value="COLLECTION">Centro de acopio</option><option value="SHELTER">Refugio o albergue</option></select></label><label className={label}>Nombre del lugar<input className={input} name="name" required/></label><label className={label}>Departamento<input className={input} name="department" required/></label><label className={label}>Municipio<input className={input} name="municipality" required/></label><label className={label}>Barrio o zona<input className={input} name="area"/></label><label className={label}>Dirección<input className={input} name="address" required/></label><label className={label}>Punto de referencia<input className={input} name="reference"/></label><label className={label}>Horario<input className={input} name="schedule"/></label><label className={label}>Teléfono<input className={input} name="phone"/></label><label className={label}>Capacidad aproximada<input className={input} type="number" name="capacity"/></label><label className={`${label} wide`}>Servicios disponibles (separados por comas)<input className={input} name="services"/></label><label className={`${label} wide`}>Qué recibe o necesita (separado por comas)<input className={input} name="items"/></label><label className={`${label} wide`}>Observaciones<textarea className={input} name="observations" rows={3}/></label></div></ManagedForm></div>}
